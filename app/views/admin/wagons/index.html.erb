<p id="notice"><%= notice %></p>

<h1>Вагони</h1>

<table>
  <thead>
  </thead>

  <tbody>
    <% @wagons.each do |wagon|%>
    <tr>
      <td>Тип вагона: <%= wagon.type %>, номер вагона: <%= wagon.number_wag%></td>
      <td><%= link_to t('common.show'), admin_wagon_path(wagon), class: "buttonItem" %></td>
      <td><%= link_to t('common.delete'), admin_wagon_path(wagon), class: "buttonItem", method: :delete, data: { confirm: 'Ви впевнені?' }%></td>
    </tr>
    <%end%>
    
  </tbody>
</table>

<br>

<%= link_to 'Новий вагон', new_admin_wagon_path, class: "buttonItem" %>
<%= link_to t('common.back'), wellcome_index_path, class: "buttonItem" %>

<tr>
  <td><%= wagon.type %></td>
  <td><%= link_to t('common.show'), admin_wagon_path(wagon) %></td>
  <td><%= link_to t('common.edit'), edit_admin_wagon_path(wagon) %></td>
  <td><%= link_to t('common.delete'), admin_wagon_path(wagon), method: :delete, data: { confirm: 'Ви впевнені?' } %></td>
</tr>
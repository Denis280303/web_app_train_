<%= form_for [:admin, route], url: admin_route_update_title_path(route), html: {method: :patch, style: "display: #{hidden ? 'none' : 'block'}"} do |f| %>
    <div class="field">
      <%= f.text_field :title %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
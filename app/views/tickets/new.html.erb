<h1>Купити білет</h1>

<%= form_for [@train, @ticket] do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(train.errors.count, "error") %> prohibited this train from being saved:</h2>

      <ul>
        <% train.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :start_station, value: @start_station.id %>
    <%= f.hidden_field :end_station, value: @end_station.id %>
  </div>


  <div class="field">
    <%= f.label :user_first_name, "Ім'я:" %><br>
    <%= f.text_field :user_first_name %>
  </div>

  <div class="field">
    <%= f.label :user_last_name, "Прізвище:" %><br>
    <%= f.text_field :user_last_name %>
  </div>

  <div class="field">
    <%= f.label :user_fathers_name, 'По-батькові:' %><br>
    <%= f.text_field :user_fathers_name %>
  </div>

  <div class="field">
    <%= f.label :user_passport, 'Паспортні дані:' %><br>
    <%= f.text_field :user_passport %>
  </div>

  <div class="field">
    <%= f.submit 'Cтворити білет' %><br>
  </div>

<% end %> 